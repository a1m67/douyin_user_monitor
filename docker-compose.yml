services:
  short-drama-tracker:
    build: .
    container_name: douyin-short-drama-tracker
    restart: unless-stopped
    ports:
      - "8900:8900"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./config:/app/config
